# Configurações do sagep-auth-cli
# Copie este arquivo para .env e preencha com os valores corretos

# URL base do serviço sagep-auth (obrigatório)
SAGEP_AUTH_URL=https://auth.sagep.com.br

# Token de autenticação para a API do sagep-auth (obrigatório)
SAGEP_AUTH_TOKEN=seu-token-aqui

# ============================================
# Configurações opcionais do ambiente Go
# ============================================

# GOPRIVATE: Lista de módulos privados (separados por vírgula)
# Se configurado, o Go tentará autenticar mesmo para repositórios públicos
# Exemplo: GOPRIVATE=github.com/empresa1,github.com/empresa2
# Para repositórios públicos, deixe vazio ou não defina
# NOTA: Esta é uma variável do Go, não do CLI. Configure via: go env -w GOPRIVATE=""
# GOPRIVATE=
